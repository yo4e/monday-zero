# Study 003 Cycle 1 Setup Audit

_Date: 2026-07-21 (Asia/Tokyo)_

## Activation decision

The frozen proposal was independently re-evaluated before activation.

Decision: **GO without proposal changes.**

The design is not limited to event-kind ordering because its frozen requirements include scope equality, single-use token state, numeric cap usage, artifact digest identity, evidence invalidation, correction lifecycle, and cycle-token consumption. The independent oracle and weak baseline create falsifiable comparison points. Conceptual overfit to Study 002 remains a limitation, but historical traces are delayed until after a synthetic gate and validator freeze, and the study has a four-cycle stop rule.

## Scope completed

Cycle 1 performed only:

- activation of the unchanged proposal as `PROTOCOL.md`;
- declarative contract, event, expected-result, and corpus schema;
- deterministic canonical JSON serialization and SHA-256 calculation;
- deterministic generation of the frozen 36 synthetic traces;
- checked-in machine-readable corpus bundle;
- regeneration and structural tests;
- state, issue, and handoff synchronization.

It did not implement validator verdict logic, the independent oracle, baseline execution, historical trace encoding, or any external action.

## Frozen corpus result

- Minimal valid traces: 6.
- Minimal invalid traces: 6.
- Composite valid traces: 4.
- Mutants: 20.
- Total: 36.
- Valid: 10.
- Invalid: 26.
- Mutation operators: 5, each represented exactly 4 times.
- Total events: 528.
- Maximum events in a trace: 20.
- Canonical SHA-256: `b7675cd11bf808a02579cc56d26252ca636e9627d9542d8d063e6752374b7d84`.

Expected mutant first violations are frozen as:

- prerequisite omission: index 4, D1;
- adjacent dependency inversion: index 2, D3;
- unauthorized insertion: index 8, D2;
- cap violation: index 4, D3;
- undisclosed correction: index 16, D4.

## Verification

Local reconstruction executed:

- `PYTHONPATH=src pytest -q tests/test_protocol_integrity_corpus.py` — **8 passed**;
- `PYTHONPATH=src python -m compileall -q src tests experiments` — no errors;
- two in-process generations produced byte-identical canonical bytes;
- the checked-in JSON bundle matched regenerated canonical bytes;
- trace arithmetic, IDs, contiguous indices, mutation inventory, class coverage, event limits, and absence of validator/historical output were tested.

Fresh-clone and full-repository regression verification were not performed in this cycle. The repository has previously lacked a recorded GitHub Actions workflow. These limits do not turn the corpus generator into validated verdict logic; Cycle 2 remains the first correctness test of protocol semantics.

## Human intervention

Yoshie Yamada supplied the plain `承認` trigger, classified as A1 access assistance. Templex selected the activation disposition, schema representation, serialization, generator implementation, tests, interpretation, and next bounded work. No human selected fixture results or validator behavior.

## Connector corrections

- Issue #8 and Issue #9 were accidentally created while switching from issue creation to repository file creation. Both were immediately renamed as accidental connector tests, closed `not_planned`, and contain no research task, evidence, authorization, or result.
- The normal contents API rejected `c4.json` because a frozen synthetic scope label was mistaken for an external-action instruction. The exact pre-generated bytes were instead stored through Git data blob/tree/commit operations and then re-fetched. The fixture semantics and bytes were not changed.

These corrections remain visible and do not affect the activation decision, corpus contents, expected verdicts, or tests.
